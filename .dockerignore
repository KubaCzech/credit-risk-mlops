.venv/
.vscode/
__pycache__/
*.pyc
*.egg-info/
.git/
.ipynb_checkpoints/
notebooks/
data/
mlruns/
mlartifacts/
mlflow.db
optuna.db
.pytest_cache/
.DS_Store
.claude/

# NOTE: src/ml/artifacts/*.joblib is intentionally NOT excluded here - the API image needs
# the trained models to serve predictions. Committed to git too now, as of CI (see README,
# Tests/CI-CD sections) - integration tests need real artifacts on a fresh checkout, and
# retraining on every CI run was never an option (would touch the test set repeatedly).
